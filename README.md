## learning-webpack-for-php

Self learning on how to use Webpack for PHP (MVC) project.

## Installation

Create a database.

Import the `users.sql` file in `database` folder.

Change your database settings at `config/database.php`.

Run Composer:

    composer install

Run NPM:

    npm install

Build:

    npm run build
